runtime: custom
env: flex

service: fastapi-service

resources:
  cpu: 2
  memory_gb: 2
  disk_size_gb: 10

automatic_scaling:
  min_num_instances: 1
  max_num_instances: 3
  cool_down_period_sec: 120
  cpu_utilization:
    target_utilization: 0.6

network:
  session_affinity: false
